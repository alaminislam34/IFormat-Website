enum Role {
  CANDIDATE
  EMPLOYER
  ADMIN
}

enum ApplicationStatus {
  SUBMITTED
  SCREENED
  SHORTLISTED
  INTERVIEWING
  OFFERED
  HIRED
  REJECTED
}

enum JobStatus {
  DRAFT
  PUBLISHED
  ARCHIVED
  CLOSED
}

enum BookingStatus {
  PENDING
  CONFIRMED
  CANCELLED
  COMPLETED
}

enum OtpType {
  EMAIL_VERIFICATION
  PASSWORD_RESET
}

enum SubscriptionStatus {
  TRIALING
  ACTIVE
  PAST_DUE
  CANCELED
  UNPAID
  EXPIRED
  INCOMPLETE
}

enum PlanBillingInterval {
  MONTHLY
  YEARLY
}

enum PlanAudience {
  EMPLOYER
  CANDIDATE
  BOTH
}

enum AuditAction {
  USER_BANNED
  USER_UNBANNED
  USER_SOFT_DELETED
  USER_RESTORED
  USER_EMAIL_VERIFIED
  JOB_STATUS_CHANGED
  JOB_FEATURED_TOGGLED
  JOB_SOFT_DELETED
  JOB_RESTORED
  PLAN_CREATED
  PLAN_UPDATED
  PLAN_SOFT_DELETED
  PLAN_RESTORED
  SUBSCRIPTION_MANUALLY_ASSIGNED
  COMPANY_VERIFIED
  COMPANY_UNVERIFIED
  SETTINGS_UPDATED
}
